# Render blueprint for the Kind Chore Tracker static site
services:
  - type: static_site
    name: kind-chore-tracker
    rootDir: kind-chore-tracker
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: NODE_VERSION
        value: "20"
